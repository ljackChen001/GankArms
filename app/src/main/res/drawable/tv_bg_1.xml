<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 实心 -->
    <!--<solid android:color="@android:color/white" />-->
    <!-- 边框 -->
    <stroke
        android:width="0.5dp"
        android:color="@color/color_fdac29"/>
    <!-- 圆角 -->
    <corners android:radius="2dp"/>
    <!-- 边距 -->
    <padding
        android:bottom="1dp"
        android:left="5dp"
        android:right="5dp"
        android:top="1dp"/>
</shape>